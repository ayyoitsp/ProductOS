---
# ⛔ Refused by the schema, and every one of these shipped before the floors moved to where the
# fields actually live.
#
#  - `told: tbd` on a RULE's shared vocabulary — the message every exchange the selector
#    reaches shows a person. The floor was written into `SlotFill`, which a rule's outcomes
#    never pass through.
#  - `about` on a settled standing, at the rule grain — the third grain `refineStanding`'s own
#    comment names, and the one it was not called at. It was invisible to every surface while
#    being inside the acceptance hash, so editing it told a reviewer a sentence had changed and
#    showed them nothing that had.
id: placeholders
in: foil
statement: A rule whose shared vocabulary says nothing and whose standing hides a residual.
fills: [refuses]
mode: supplies
standing:
  kind: stated
  about: whether this rule means anything at all
scope:
  asked_by: person
  everywhere: true
outcomes:
  - name: something-or-other
    when: something happens that nobody has pinned down
    told: tbd
criteria:
  - id: 1
    slot: refuses
    kind: conformance
    then: nothing anybody could check
---

Refused before it loads.
